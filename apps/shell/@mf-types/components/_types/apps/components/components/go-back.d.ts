/// <reference types="react" />
import { NextRouter } from 'next/router';
export interface GoBackProps {
    router: NextRouter;
}
export declare const GoBack: ({ router }: GoBackProps) => JSX.Element;
